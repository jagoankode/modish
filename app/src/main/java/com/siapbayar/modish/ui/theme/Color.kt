package com.siapbayar.modish.ui.theme

import androidx.compose.ui.graphics.Color

val PinkPrimary  = Color(0xFFEA3A60)
val PinkSecondary  = PinkPrimary.copy(alpha = 0.75f)
val PinkTertiary  = PinkPrimary.copy(alpha = 0.62f)

val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

val Grey = Color(0xFF3C3C43)